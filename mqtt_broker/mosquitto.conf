# Listener on port 1883 (default MQTT port)
listener 1883

# Path to the password file
password_file /mosquitto/config/mosquitto_passwd

# Allow anonymous connections (false means disabled)
allow_anonymous false
