# Use the official PostgreSQL image from Docker Hub
FROM postgres:latest

# Set environment variables
ENV POSTGRES_USER=developer
ENV POSTGRES_PASSWORD=pw
ENV POSTGRES_DB=poseidon

# Expose the PostgreSQL port
EXPOSE 5432

# Include initialization scripts
# COPY ../database_init/init.sql /docker-entrypoint-initdb.d/

# Optionally, you can customize the PostgreSQL version
# ENV POSTGRES_VERSION=12

# Optionally, you can specify the PostgreSQL data directory
# VOLUME /var/lib/postgresql/data